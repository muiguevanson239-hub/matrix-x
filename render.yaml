services:
  - type: web
    name: matrix-x
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn api.matrix_api:app --host 0.0.0.0 --port 10000
    envVars:
      - key: PYTHON_VERSION
        value: 3.10